# 이력관리 - 구글시트 연동 설정 가이드 (최초 1회)

처음 설정하시는 분을 위해 단계별로 자세히 적었습니다. 순서대로만 따라 하시면 됩니다.
전체 소요시간 약 10분.

---

## 1단계. 구글시트 만들기

1. 구글 드라이브(drive.google.com)에서 **새로 만들기 > Google 스프레드시트** 클릭
2. 이름을 `서울숲엘타워_이력관리` 처럼 알아보기 쉽게 변경
3. 화면 하단의 시트 탭 이름을 더블클릭해서 **`이력관리`** 로 변경
   (이름이 다르면 코드가 자동으로 첫 번째 시트를 대신 사용하지만, 헷갈리지 않게 맞춰두는 걸 권장합니다)
4. 1행(맨 윗줄)에 아래 7개 항목을 순서대로 입력

   | A | B | C | D | E | F | G |
   |---|---|---|---|---|---|---|
   | 등록시각 | 날짜 | 구분 | 위치 | 작업내용 | 처리자 | 비고 |

   ※ 이 헤더 이름이 웹페이지 표의 컬럼명과 그대로 연결되니 정확히 입력하세요.

---

## 2단계. Apps Script 코드 붙여넣기

1. 방금 만든 스프레드시트에서 상단 메뉴 **확장 프로그램(Extensions) > Apps Script** 클릭
   → 새 탭에서 스크립트 편집기가 열립니다.
2. 기본으로 열려있는 `Code.gs` 파일의 내용을 전부 지우고,
   제가 드린 **`code.gs`** 파일의 내용을 그대로 복사해서 붙여넣습니다.
3. 상단의 **저장(디스크 모양 아이콘)** 클릭. 프로젝트 이름을 물어보면 `이력관리 API` 등으로 저장.

---

## 3단계. 웹앱으로 배포하기

1. 화면 우측 상단의 **배포(Deploy) > 새 배포(New deployment)** 클릭
2. "유형 선택" 옆의 톱니바퀴 아이콘 클릭 → **웹 앱(Web app)** 선택
3. 아래처럼 설정합니다.
   - **설명**: `이력관리 v1` (아무 이름이나 가능)
   - **실행 사용자(Execute as)**: **나(본인 계정)**
   - **액세스 권한이 있는 사용자(Who has access)**: **전체(Anyone)**
     (구글 로그인 없이도 폼 제출이 되게 하려면 이 옵션이 필요합니다.
     시트 자체는 여전히 본인 계정으로만 접근 가능하니 안전합니다.)
4. **배포(Deploy)** 클릭
5. "권한 승인이 필요합니다" 화면이 뜨면:
   - **권한 승인(Authorize access)** 클릭
   - 본인 구글 계정 선택
   - "Google에서 확인하지 않은 앱입니다" 경고가 뜨면 → 하단의 **고급(Advanced)** 클릭
     → **`이력관리 API`(안전하지 않음)로 이동** 클릭 (본인이 만든 스크립트라 안전합니다)
   - 권한 목록 확인 후 **허용(Allow)** 클릭
6. 배포가 완료되면 **웹 앱 URL**이 표시됩니다.
   `https://script.google.com/macros/s/AKfycb.../exec` 형태의 주소를 **복사**하세요.

---

## 4단계. 웹페이지에 URL 연결하기

1. `history.html` 파일을 텍스트 편집기로 엽니다.
2. 파일 맨 아래쪽 `<script>` 안에서 아래 줄을 찾습니다.

   ```js
   const SCRIPT_URL = "여기에_배포된_웹앱_URL을_붙여넣으세요";
   ```

3. 따옴표 안을 3단계에서 복사한 URL로 교체합니다.

   ```js
   const SCRIPT_URL = "https://script.google.com/macros/s/AKfycbxxxxxxxxxx/exec";
   ```

4. 저장 후 GitHub에 커밋 · 푸시하면 바로 반영됩니다.
   (또는 로컬에서 파일을 열어 먼저 테스트해봐도 됩니다.)

---

## 5단계. 테스트

1. `history.html` 페이지를 열고 상단의 노란색 안내 문구가 사라졌는지 확인
2. 이력 입력 폼에 테스트 데이터를 하나 넣고 **이력 저장** 클릭
3. "저장되었습니다" 메시지가 뜨고, 아래 조회 표에 방금 입력한 내용이 나타나면 성공
4. 실제 구글시트를 열어서도 새 행이 추가됐는지 확인

---

## 나중에 code.gs를 수정했다면?

Apps Script 코드를 나중에 수정하는 경우, **배포 > 배포 관리(Manage deployments) > 연필 아이콘(수정) > 버전: 새 버전(New version) > 배포**를 다시 해줘야
수정한 내용이 실제 웹앱에 반영됩니다. (URL은 그대로 유지됩니다.)

---

## 자주 발생하는 오류

| 증상 | 원인 / 해결 |
|---|---|
| "이력을 불러오지 못했습니다" | SCRIPT_URL이 아직 기본값이거나 오타 → 4단계 다시 확인 |
| 저장은 되는데 목록에 안 보임 | 시트 헤더(1행) 이름이 코드와 다름 → 1단계 표와 정확히 일치하는지 확인 |
| 배포 시 권한 오류 | 배포 설정에서 "액세스 권한: 전체(Anyone)"로 되어 있는지 확인 |
| 코드 수정했는데 반영 안 됨 | "새 배포"가 아니라 "배포 관리 > 새 버전"으로 재배포해야 함 |
